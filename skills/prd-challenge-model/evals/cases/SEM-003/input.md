# Scope

本迭代支持用户手动保存草稿。

# Requirements

- RQ-DRAFT-01：用户点击“保存草稿”时提交当前表单。
- RQ-DRAFT-02：仅当服务端返回 `saved=true` 且返回草稿 ID 时，页面显示“已保存”，并在草稿列表中显示同一 ID；否则显示“保存失败”，保留表单内容。
- AC-DRAFT-01：构造服务端返回 `saved=true` 且含草稿 ID 的响应，验证页面显示“已保存”且列表出现同一 ID；其他响应不得显示“已保存”。
